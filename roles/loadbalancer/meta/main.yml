---
# Role metadata
galaxy_info:
  role_name: loadbalancer
  author: Marija
  description: HAProxy load balancer configuration with health checks and stats
  company: Configuration Management Course
  license: MIT
  min_ansible_version: "2.15"
  
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
    - name: Debian
      versions:
        - bullseye
        - bookworm

  galaxy_tags:
    - loadbalancer
    - haproxy
    - proxy
    - load-balancing
    - high-availability

# Dependencies - loadbalancer depends on common role
dependencies:
  - role: common
    tags:
      - common